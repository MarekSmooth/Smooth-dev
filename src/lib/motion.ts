// Shared motion tokens — keep every animation on the same rhythm (UI/UX Pro Max: motion-consistency).
export const EASE_OUT: [number, number, number, number] = [0.16, 1, 0.3, 1];

export const SPRING_SNAPPY = { type: 'spring', stiffness: 300, damping: 24, mass: 0.8 } as const;

export const TAP_SCALE = { scale: 0.97 };
